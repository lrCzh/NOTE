<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/transparent</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="shapeAppearanceLargeComponent">@style/MyShapeAppearanceLargeComponent</item>
        <!--tips-->
        <!--shapeAppearanceSmallComponent 在Button, Chip, Text的属性中使用, 默认 4dp 的圆角-->
        <!--shapeAppearanceMediumComponent 在Card, Dialog, Date Picker中使用, 默认 4dp 的圆角-->
        <!--shapeAppearanceLargeComponent 在BottomSheet中使用, 默认 0dp 圆角-->

    </style>

    <!--CollapsingToolbarLayout折叠时标题文字样式-->
    <style name="CollapsedTitleTextStyle" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/transparent</item>
    </style>

    <!--loading弹窗样式-->
    <style name="LoadingDialogTheme" parent="android:Theme.Holo.Light.Dialog">
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--设置dialog的背景-->
        <item name="android:background">@android:color/transparent</item>
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--如果背景需要模糊，模糊到什么程度，当值为1 时，除了dialog以外的部分全黑-->
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <!-- AddNoteDialog进出动画 -->
    <style name="ActionSheetDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/sheet_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/sheet_dialog_out</item>
    </style>

    <style name="MyShapeAppearanceLargeComponent" parent="ShapeAppearance.MaterialComponents.LargeComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

</resources>